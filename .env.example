# 招待 URL は build 時に JS バンドルへインラインされる（`NEXT_PUBLIC_*` は静的書き出しでは runtime 変更不可）。
# 未設定でもビルドが落ちないよう、未設定時は Header / Footer の Discord CTA をプレースホルダ表示にする。
#
# 招待コードのみを渡すと壊れるため、完全 URL (`https://discord.gg/...`) を設定すること。
NEXT_PUBLIC_DISCORD_INVITE="https://discord.gg/qmCGSBmc28"
